{{!-- display single post --}}
<article>
  <div>
    <h2>{{title}}</h2>
  </div>
  <div>
     by {{user.username}} on {{format_date created_at}}
  </div>
  <div>
  <p>{{post_content}}</p>
  </div>
</article>